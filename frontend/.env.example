# Google OAuth web client ID from Google Cloud Console
VITE_GOOGLE_CLIENT_ID=

# Optional override when backend does not run on port 8080
VITE_API_BASE_URL=

# Optional Vite proxy target for /api, for example http://localhost:8081
VITE_BACKEND_TARGET=
